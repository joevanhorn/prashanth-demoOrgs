"""
ITP (Identity Threat Protection) Demo Automation

This package provides tools for Okta ITP demonstrations:

- session_authenticator: Headless browser auth to capture session cookies
- session_replayer: Cookie replay from a different context to trigger detection
- ssf_provider: SSF security events provider registration and signal sending
"""
